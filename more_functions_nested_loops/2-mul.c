#include "main.h"
/**
 * mul - multiplie 2 variables
 * @a: le nom de la variable
 * @b: le nom de l'autre variable
 *
 * Return: renvoie la multiplication des deux variables
 */

int mul(int a, int b)
{
return (a * b);
}
